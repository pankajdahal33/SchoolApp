import 'package:flutter/material.dart';

class TeacherHomeWork extends StatelessWidget {
  const TeacherHomeWork({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Teacher Home Work'),
      ),
    );
  }
}
