import 'package:flutter/material.dart';

class TeacherNotification extends StatelessWidget {
  const TeacherNotification({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Teacher Notification'),
      ),
    );
  }
}
