import 'package:flutter/material.dart';

class TeacherAcademic extends StatelessWidget {
  const TeacherAcademic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Teacher Academic'),
      ),
    );
  }
}
