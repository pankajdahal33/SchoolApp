import 'package:flutter/material.dart';

class StudentRoutine extends StatelessWidget {
  const StudentRoutine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Student Routine'),
      ),
    );
  }
}
