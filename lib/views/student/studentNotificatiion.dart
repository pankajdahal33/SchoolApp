import 'package:flutter/material.dart';

class StudentNotification extends StatelessWidget {
  const StudentNotification({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Student Notification'),
      ),
    );
  }
}
